DROP TABLE IF EXISTS `person`;
CREATE TABLE `person` (
 id int(5) NOT NULL AUTO_INCREMENT,
 name VARCHAR(20) NOT NULL,
 sex enum('男','女')DEFAULT NULL,
 age int(3) NOT NULL,
 Height_cm int(5),
 Job VARCHAR(20) NOT NULL,
 FaceaLevel int(5),
 CreatTime VARCHAR(22),
 PRIMARY KEY (`id`)
) ENGINE=InnoDB AUTO_INCREMENT=5 DEFAULTCHARSET=utf8;



数据库可能有点问题，不能加编码，还是很有趣的